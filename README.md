# Borough Yards Custom `

## Explore 
To run

```
    cd explore
    pnpm install
    ./esbuild -w
```

Then open `localhost:8000` in your browser

## Book
To run

```
    cd book 
    pnpm install
    ./esbuild -w
```

Then open `localhost:8000` in your browser

